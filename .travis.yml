sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t Marjook/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true Marjook/docker-react npm run test
